---
// How It Works — 3-step process to reduce booking anxiety
import homeContent from '../content/home.json';
import siteContent from '../content/site.json';

const howItWorks = homeContent.howItWorks;

const stepIcons = [
  `<svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"/></svg>`,
  `<svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/></svg>`,
  `<svg class="w-8 h-8" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>`
];
---

<section class="py-20 lg:py-28 bg-[#F5F5F5]" aria-label="How it works">
  <div class="relative max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">

    <!-- Section header -->
    <div class="text-center mb-14 lg:mb-16 max-w-2xl mx-auto">
      <p class="text-[#E31E24] font-semibold text-xs uppercase tracking-[0.2em] mb-3">{howItWorks.tagline}</p>
      <h2 class="font-display text-3xl sm:text-4xl lg:text-5xl font-bold text-[#1A1A1A] leading-tight">
        {howItWorks.heading}
      </h2>
      <p class="mt-5 text-gray-600 text-lg leading-relaxed">
        {howItWorks.subtext}
      </p>
    </div>

    <!-- Steps -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-10 md:gap-8 relative">

      <!-- Connector line (desktop only) -->
      <div class="hidden md:block absolute top-12 left-[16.66%] right-[16.66%] h-px bg-gradient-to-r from-transparent via-[#E31E24]/40 to-transparent z-0 pointer-events-none"></div>

      {howItWorks.steps.map((step, i) => (
        <div class="relative z-10 flex flex-col items-center text-center">
          <div class="relative mb-6">
            <div class="w-24 h-24 rounded-full border-2 border-[#E31E24]/20 flex items-center justify-center bg-white shadow-sm">
              <div class="text-[#E31E24]" set:html={stepIcons[i]} />
            </div>
            <span class="absolute -top-1 -right-1 w-8 h-8 bg-[#E31E24] text-white text-xs font-bold rounded-full flex items-center justify-center font-display shadow-md">
              {step.number}
            </span>
          </div>

          <h3 class="font-display font-bold text-[#1A1A1A] text-xl mb-3">{step.title}</h3>
          <p class="text-gray-600 text-sm leading-relaxed max-w-xs">{step.desc}</p>
        </div>
      ))}
    </div>

    <!-- CTA below steps -->
    <div class="text-center mt-14">
      <a
        href={`tel:${siteContent.phoneTel}`}
        class="inline-flex items-center gap-2 bg-[#E31E24] hover:bg-red-700 text-white font-bold text-lg px-10 py-4 rounded-xl transition-all hover:scale-105 hover:shadow-[0_0_24px_rgba(227,30,36,0.35)]"
      >
        <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
          <path d="M6.6 10.8c1.4 2.8 3.8 5.1 6.6 6.6l2.2-2.2c.3-.3.7-.4 1-.2 1.1.4 2.3.6 3.6.6.6 0 1 .4 1 1V20c0 .6-.4 1-1 1-9.4 0-17-7.6-17-17 0-.6.4-1 1-1h3.5c.6 0 1 .4 1 1 0 1.3.2 2.5.6 3.6.1.3 0 .7-.2 1L6.6 10.8z"/>
        </svg>
        Book Now — {siteContent.phone}
      </a>
    </div>

  </div>
</section>
